#include <iostream>
#include <string>
using namespace std;

struct Node {
    string str;
    Node *prev;
    Node *next;
};

int n;


int main() {
    // Please write your code here.
    Node cur;
    cin >> cur.str;
    cin >> n;
    for (int i = 0; i < n; i++) {
        int cmd;
        cin >> cmd;
        if (cmd == 1) {
            Node
        }
    }
    return 0;
}